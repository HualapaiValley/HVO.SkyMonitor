@page "/login"
@using HVO.SkyMonitor.CameraAgent.Components.Account.IdentityShared

<PageTitle>Sign in</PageTitle>

@* This stub renders no heading by design. It exists only to redirect to
   /Account/Login and has no content a user reads, so an h1 here would add a
   heading nobody reaches and make the redirect briefly announce itself as a
   page. The same exemption covers any future redirect-only stub. The real
   sign-in page, Account/Pages/Login.razor, carries the h1 for this flow. *@
<RedirectToLogin />