REVIEW PR-910-R0-f95a46ee
Level: Deep
Mode: Initial
Range: development/v1..f95a46eeba8d76c222d24bfa31331d0faa0a5549 (complete PR diff)
Reviewer: opencode:openai:local:promotion-review, posted as hvo-agentcontrol[bot]
Provider/model/effort: openai / gpt-5.6-sol / high
Risk lens: ci-control

Verified mechanically and by live probe with a read-only App token:
- token permission set is metadata/contents/actions read + pull-requests write; no merge endpoint, no push, no `merge_method` anywhere in the file
- the behind-check exits 1 inside the compare step, before either PR-mutating step, and both later steps are additionally gated on `nothing_to_promote`
- the green-run query filters `head_sha` **and** `event=push`, and on the current v1 head returns the push run (#64, success), not a PR run
- the existing-PR lookup filters `base=main&head=<owner>:development/v1`; GitHub matches `head` by ref, so a refreshed PR follows the branch head and the PATCH only touches title/body
- live compare on the real branches: `ahead_by=21 behind_by=0`, no open promotion PR, so the first dispatch will open one
- no `${{ }}` inside any `run:` block; actionlint clean; the heredoc terminator lands at column 0 after YAML dedent (`bash -n` on the extracted step passes)
- cron `0 9 * * *` is 02:00 America/Phoenix year-round (no DST), matching the documented time
- `concurrency: promote-main` without cancel-in-progress, so a dispatch during the scheduled run queues rather than races it

Findings: none.
Verdict: APPROVE — CONVERGED at f95a46eeba8d76c222d24bfa31331d0faa0a5549.
