REVIEW PR-919-R0-30d55828
Level: Deep
Mode: Initial (with F1 corrected on this head)
Range: development/v1..30d558282cba1e197140d68a4a30361b4034ac0f (complete PR diff)
Reviewer: opencode:openai:local:backup-review, posted as hvo-agentcontrol[bot]
Provider/model/effort: openai / gpt-5.6-sol / high
Risk lens: data-loss, durability

Verified mechanically:
- a damaged backup cannot reach the swap: every bucket stages completely, with every data file re-hashed and every descriptor checked against the inventory, before the first rename; the test proves the live bucket is byte-identical after a refused restore
- the inventory is refused by checksum before any entry is read; schema and count are checked after
- no state exists in which a bucket is a mixture: the swap is two renames per bucket, and the previous bucket is deleted only after the restored one is in place
- backup cannot copy through a link (the #592 root refuses resolution; tested with nothing copied), cannot record a corrupt object (length and digest checked while copying, the partial target removed), cannot merge into a non-empty target, and cannot silently omit a configured bucket
- the S3 path is untouched: the modes exit 2 before any store is constructed when the provider is not Filesystem; S3ObjectStore and its DI are not in the diff
- Main's return-type change is exercised by the database-initialization acceptance test in LogicHost Integration (414/414)
- the backup path is refused inside the root, so a backup cannot be enumerated as bucket content

F1 (High): an interruption between the two swap renames left no live bucket and the next restore refused with an instruction to remove the only remaining copy; the leftover staging directory was deleted recursively without a link check. VERIFIED_CORRECTED at this head: the heal branch restores the previous bucket when the live one is absent, refuses only when both exist, and every Move/Delete is preceded by RefuseLink; three tests pin the window, the refusal, and the link.

Findings: 1 High, corrected and verified before this verdict.
Verdict: APPROVE — CONVERGED at 30d558282cba1e197140d68a4a30361b4034ac0f.
