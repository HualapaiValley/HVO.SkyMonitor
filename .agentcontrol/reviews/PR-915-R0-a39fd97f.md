REVIEW PR-915-R0-a39fd97f
Level: Deep
Mode: Initial
Range: development/v1..a39fd97f9d6c1042cf60ad5bb8de7d36e6d335fc (complete PR diff)
Reviewer: opencode:openai:local:objstore-review, posted as hvo-agentcontrol[bot]
Provider/model/effort: openai / gpt-5.6-sol / high
Risk lens: durability

Verified mechanically, not from the PR text:
- `git grep s3://` over `src/` returns nothing; the six producers now share one `LogicalScheme` constant and `ArtifactPrefix` has no provider branch, so the persisted identity cannot vary by provider
- `S3ObjectStore.cs` is untouched; `S3ObjectStoreClientFactory.CreateConfig` and the credential-mode switch are byte-identical to base apart from the parameter type, so the S3 transport is the same transport
- both validators (`HasExclusiveProviderSettings`, `HasValidFilesystemRoot`) are wired into the `ValidateOnStart` chain; the unit tests exercise both directions of the exclusion and the absolute/normalized root rule
- all ten flattened keys the deployment inventory writes (`docker-compose.apps.yml`, `scripts/deploy/up.sh`) still bind; the new binder test proves grouped and flattened keys both land in the S3 group and that the resulting contradiction is refused
- the canonical migration is unchanged and the `StorageReference` columns carry no scheme constraint, so "one canonical migration, no alias" holds by inspection
- the health-check rename is applied at registration, in the Common observability data-key map, and in the outage acceptance test; `git grep s3-object-store` leaves only the S3 adapter's own performance-evidence file names and a recorded evidence JSON
- telemetry: the new `object_store.provider` tag is added to the bounded-dimension allowlist test and asserted equal to the enum name, so it cannot carry an endpoint or path
- the #592 boundary test refuses documentation-before-existence today and would refuse any non-empty reference set or forbidden package on arrival
- Tier C evidence reproduced: Release/Debug warning-clean, format, package audit, category audit 3502/676/107/1/0/1, Unit 3495+skips/0 failed, Integration 676/676 across six assemblies, canonical migration, classification

One observation, not a finding: `HasS3Settings` treats a non-default `Region` as "S3 configured". A future Filesystem deployment that inherits `ObjectStorage__Region` from a stale inventory will fail closed at startup with the exclusivity message. That is the intended behaviour (a half-migrated inventory should not start), and #506 owns removing the S3 keys from the inventory; noting it so #506 does not rediscover it.

Findings: none.
Verdict: APPROVE — CONVERGED at a39fd97f9d6c1042cf60ad5bb8de7d36e6d335fc.
