# Issue 256 Candidate Evidence

## Candidate

- Base revision: `85694f432742f5ae258035406502dbc56c2c0dc4`
- SDK: `10.0.100`
- SQL Server: Testcontainers SQL Server 2022 used by `HVO.SkyMonitor.IntegrationTests`
- Idempotent SQL SHA-256: `e33fd0c95c54f26a4fee2d1e4bd47beefb301b85964572120b904e4819c8acd7`
- Generated by: `./scripts/logichost:generate-migration-sql /tmp/opencode/issue-256-logichost-idempotent.sql`

## Database Results

`DatabaseInitializationAcceptanceTests` passed all seven scenarios in Release:

- clean and current initialization convergence;
- interrupted `Running` state rejection and successful restart;
- held application lock rejection without durable-state mutation, followed by successful retry;
- legacy database convergence;
- clean initialization through the shipped migration role and a distinct SQL login;
- complete idempotent SQL execution against clean and current states;
- shipped runtime role read/write/application-lock success, control-table and DDL denial, overprivilege rejection, and production host-mode execution/exit.

One representative clean initialization recorded:

| Metric | Result |
| --- | ---: |
| Command elapsed | 6.396 s |
| Initial data allocation | 8,388,608 bytes |
| Final data allocation | 8,388,608 bytes |
| Data-file growth | 0 bytes |
| Initial log allocation | 8,388,608 bytes |
| Final log allocation | 8,388,608 bytes |
| Log-file growth | 0 bytes |
| Data autogrowth setting | 8,192 pages / 64 MiB, fixed |
| Log autogrowth setting | 8,192 pages / 64 MiB, fixed |
| Observed autogrowth events | 0; allocated file sizes were unchanged |
| Final volume headroom | 26,000,404,480 bytes |
| Lock waiters under contention | 0; the second zero-timeout command failed before mutation |

The migration and runtime connections asserted stable application names
`HVO.SkyMonitor.LogicHost.DatabaseInitialization` and
`HVO.SkyMonitor.LogicHost`. Evidence contains generated login names only; no
connection secrets are retained.

## Recovery And Security

- Replacing a completed marker with a stale `Running` attempt caused production validation to fail closed; rerunning initialization produced a new attempt ID and `Completed` state.
- Holding the command-wide session lock caused a concurrent initializer to fail before changing the prior completed marker; release and retry converged.
- A Production-mode host using the shipped runtime role passed environmental ingest, derivative claim, retention-reference, public network-read, Identity/OpenIddict DML, and `sp_getapplock` paths; it also held effective DML grants on every ordinary `dbo` application table.
- Runtime was denied table, sequence, and view creation and writes to both initialization-control tables.
- Adding the runtime login to `db_datawriter` caused production validation to reject it because control-table writes became effective.
- The idempotent script was executed twice after setting the required SQL session options. Historical same-batch column and trigger definitions were corrected to execute through dynamic batches.

## Validation

- Debug solution build with warnings as errors: passed.
- Release solution build with warnings as errors: passed.
- Formatting verification: passed.
- Pending EF model changes: none.
- Package audit: passed.
- Focused issue unit tests: `11/11` passed.
- Focused issue integration tests: `7/7` passed.
- Complete solution Unit gate: `1,722/1,722` passed with the invalid Docker endpoint.
- Central integration tests: `350/350` passed.
- CameraAgent integration tests: `18/18` passed.
- A complete integration run encountered the known #290 intermittent standalone owner `403`; its exact replacement test passed `1/1`.
