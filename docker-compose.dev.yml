# Legacy compatibility stub
# Infrastructure moved to docker-compose.infrastructure.yml
# Application services moved to docker-compose.apps.yml
# Use the scripts under ./scripts or pass both files to docker compose.

services: {}
