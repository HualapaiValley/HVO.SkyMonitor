# shellcheck shell=bash disable=SC2034
# Non-secret OpenCode endpoint identity. Keep the ports aligned with devcontainer.json.
OPENCODE_TMUX_SESSION="${OPENCODE_TMUX_SESSION:-opencode-skymonitor}"
OPENCODE_CONTAINER_PORT="${OPENCODE_CONTAINER_PORT:-4096}"
OPENCODE_HOST_PORT="${OPENCODE_HOST_PORT:-4097}"
