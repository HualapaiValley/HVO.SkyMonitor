# shellcheck shell=bash disable=SC2034
# Non-secret host lifecycle identity. Keep the host port aligned with appPort in devcontainer.json.
OPENCODE_TMUX_SESSION=opencode-skymonitor
OPENCODE_CONTAINER_PORT=4096
OPENCODE_HOST_PORT=4096
